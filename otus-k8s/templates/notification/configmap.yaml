apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-order-config
data:
  SERVER_PORT: '{{ .Values.service.order.port }}'
  KAFKA_BOOTSTRAP_SERVERS: my-cluster-kafka-bootstrap.kafka.svc.cluster.local:9092