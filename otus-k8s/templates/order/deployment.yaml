apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-order-deployment
  labels:
    {{- include "otus-k8s.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "otus-k8s.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      name: {{ .Chart.Name }}
      labels:
        {{- include "otus-k8s.selectorLabels" . | nindent 8 }}
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "{{ .Values.service.order.port }}"
        prometheus.io/path: "/prometheus"
    spec:
      containers:
        - name: order
          image: achernyavskiy0n/order-service:v3
          env:
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                configMapKeyRef:
                  name: {{ .Chart.Name }}-order-config
                  key: KAFKA_BOOTSTRAP_SERVERS
            - name: SERVER_PORT
              valueFrom:
                configMapKeyRef:
                  name: {{ .Chart.Name }}-order-config
                  key: SERVER_PORT
          ports:
            - name: order-port
              containerPort: {{ .Values.service.order.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health/liveness
              port: order-port
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health/readiness
              port: order-port
            initialDelaySeconds: 10
            periodSeconds: 5
            timeoutSeconds: 2
          envFrom:
            - configMapRef:
                name: {{ .Chart.Name }}-order-config